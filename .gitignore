node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Archivos generados por vite-plugin-pwa en el build
dev-dist

# Variables de entorno locales (no commitear claves)
.env
.env.local
.env.*.local

.vercel
